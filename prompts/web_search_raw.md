Search the web for: {query}

Return ONLY a JSON array of the top {max_results} results. Each item:
{"title": "...", "url": "...", "snippet": "..."}. The snippet MUST be the
search engine's verbatim excerpt — do NOT rewrite, summarize, or interpret
it. No prose, no preamble, no markdown fences. Just the JSON array.
