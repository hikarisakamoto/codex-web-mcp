{
  "name": "codex-web-mcp",
  "version": "0.1.0",
  "description": "Node/TypeScript implementation of codex-web-mcp: an MCP server proxying web access through the OpenAI Codex CLI.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "codex-web-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test --import tsx tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
